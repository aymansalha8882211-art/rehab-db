kind = "web"
previewPath = "/"
title = "Rehabilitation Database"
version = "1.0.0"
id = "artifacts/rehab-db"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18650

[services.development]
run = "pnpm --filter @workspace/rehab-db run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/rehab-db", "run", "build" ]
publicDir = "artifacts/rehab-db/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18650"
BASE_PATH = "/"
